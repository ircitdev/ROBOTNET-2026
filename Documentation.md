# Документация RoborNET 2026

RoborNET 2026 — это высокотехнологичный лендинг для интернет-провайдера в Волгограде, разработанный с использованием передовых веб-технологий и интегрированным искусственным интеллектом на базе Google Gemini.

## 🚀 Технологический стек

*   **Frontend**: React 19, TypeScript
*   **Стилизация**: Tailwind CSS (с кастомными неоновыми темами)
*   **Анимации**: GSAP (GreenSock Animation Platform) + ScrollTrigger
*   **3D Графика**: Three.js (WebGL)
*   **AI Модели**: Google Gemini 3 Flash Preview (текст) и Gemini 2.5 Flash Native Audio (голос)
*   **API**: @google/genai SDK

## 📁 Структура проекта

*   `App.tsx`: Основной компонент приложения. Содержит структуру страницы, логику прокрутки, модальные окна и баннеры.
*   `components/`:
    *   `ThreeHero.tsx`: Интерактивный 3D фон с ракетой, реагирующей на мышь и клики.
    *   `GeminiChat.tsx`: Продвинутый виджет чата с поддержкой текста, голосового ввода и живого аудио-звонка.
    *   `Preloader.tsx`: Анимированный экран загрузки с индикацией прогресса.
    *   `PromoModal.tsx`: Маркетинговое окно с акциями.
*   `services/`:
    *   `geminiService.ts`: Логика взаимодействия с текстовой моделью ИИ, включая системные инструкции и контекст компании.
*   `utils/`:
    *   `audioUtils.ts`: Утилиты для кодирования/декодирования PCM аудио для Live API.
*   `constants/` & `data`: Статические данные (тарифы, новости, контакты, список каналов).

## 🧠 Искусственный Интеллект (RoborNET AI)

### Текстовый чат
Использует модель `gemini-3-flash-preview`. В систему заложены знания о:
- Всех тарифах компании (`tariffsData.ts`).
- Сетке вещания ТВ каналов (`channels.ts`).
- Часто задаваемых вопросах (`constants.ts`).
- Контактных данных офиса.

### Голосовой ввод (Dictation)
Позволяет пользователю надиктовывать сообщения в текстовое поле. Реализовано через стандартный браузерный **Web Speech API**.

### Live Voice Mode
Уникальная функция живого общения.
- **Модель**: `gemini-2.5-flash-native-audio-preview-12-2025`.
- **Механика**: Прямое соединение через WebSockets. Аудио с микрофона пользователя в реальном времени стримится модели, которая отвечает синтезированным голосом с минимальной задержкой.
- **Транскрипция**: В процессе разговора в чате отображаются текстовые расшифровки как пользователя, так и AI.

## 🎨 Дизайн и UI/UX

- **Glassmorphism**: Использование полупрозрачных элементов с эффектом `backdrop-blur`.
- **Dark Mode First**: Сайт спроектирован с упором на темную тему, но поддерживает динамическое переключение на светлую.
- **GSAP Reveal**: Элементы плавно появляются при прокрутке страницы.
- **Интерактивность**: 3D ракета в хедере служит визуальной метафорой скорости интернета RoborNET.

## 🛠 Настройка и Конфигурация

### Изменение тарифов
Все тарифы редактируются в файле `tariffsData.ts`. Каждому тарифу можно назначить флаг `isPopular` для выделения или `isWireless` для частного сектора.

### Контакты
Все контактные данные (телефоны, адреса, ссылки на соцсети) централизованы в `constants/contacts.ts`.

### Новости
Обновление ленты новостей происходит через массив в `news.ts`. Каждая новость поддерживает "краткое описание" для карточки и "полный текст" для модального окна.

## 🛡 Безопасность и Лицензии
Проект включает баннер согласия на использование Cookie (в `App.tsx`) и ссылки на официальные документы (договор оферты, политика конфиденциальности) в футере.
